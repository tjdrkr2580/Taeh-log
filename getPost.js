import path from "path";
import fs from "fs";
import matter from "gray-matter";

const getPost = (filePath) => {
  const postDir = path.join(process.cwd(), "public");
};

export const getAllPost = async () => {};
